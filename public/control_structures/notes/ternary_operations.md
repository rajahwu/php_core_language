# ternary_operations

```php
$is_logged_in = true;

$message = $is_logged_in ? "Welcome back!" : "Hello There";
echo $message; // "welcome back!"

$name = isset($name) ? $name : 'Joe';

$name - $name ?: 'Joe';
```
